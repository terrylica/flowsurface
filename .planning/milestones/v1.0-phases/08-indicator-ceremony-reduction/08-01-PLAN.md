---
phase: 08-indicator-ceremony-reduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chart/indicator/kline.rs
  - src/chart/kline/mod.rs
  - src/chart/kline/data_ops.rs
  - src/chart/kline/odb_core.rs
  - CLAUDE.md
autonomous: true
requirements: [QUAL-03, VER-01, VER-02, VER-03]

must_haves:
  truths:
    - "A single make_indicator(which, cfg) factory function exists -- no dual-path confusion"
    - "Adding a standard subplot indicator requires exactly 3 files (2 modified + 1 new)"
    - "CLAUDE.md documents the verified 3-file checklist as the canonical reference"
    - "cargo clippy -- -D warnings passes clean"
  artifacts:
    - path: "src/chart/indicator/kline.rs"
      provides: "Consolidated make_indicator(which, cfg) factory"
      contains: "pub fn make_indicator"
    - path: "CLAUDE.md"
      provides: "Updated indicator checklist"
      contains: "3 files"
  key_links:
    - from: "src/chart/kline/mod.rs"
      to: "src/chart/indicator/kline.rs"
      via: "indicator::kline::make_indicator call"
      pattern: "indicator::kline::make_indicator"
    - from: "src/chart/kline/data_ops.rs"
      to: "src/chart/indicator/kline.rs"
      via: "make_indicator call"
      pattern: "make_indicator"
    - from: "src/chart/kline/odb_core.rs"
      to: "src/chart/indicator/kline.rs"
      via: "make_indicator call"
      pattern: "make_indicator"
---

<objective>
Consolidate the two indicator factory functions into one and document the verified 3-file indicator ceremony.

Purpose: The research debunked the "36 file" claim -- the actual ceremony is already 3 files. This plan eliminates the confusing dual-factory pattern (make_empty + make_indicator_with_config) and updates CLAUDE.md with the verified checklist.

Output: Single make_indicator factory, updated CLAUDE.md checklist, all callers migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-indicator-ceremony-reduction/08-RESEARCH.md

@src/chart/indicator/kline.rs
@src/chart/kline/mod.rs
@src/chart/kline/data_ops.rs
@src/chart/kline/odb_core.rs

<interfaces>
<!-- Key types the executor needs -->

From data/src/chart/kline.rs:

```rust
pub struct Config {
    // ... fields including:
    pub ofi_ema_period: usize,
    pub intensity_lookback: usize,
    pub anomaly_fence: bool,
}
```

From src/chart/indicator/kline.rs:

```rust
pub fn make_empty(which: KlineIndicator) -> Box<dyn KlineIndicatorImpl>
// ^ This gets replaced by make_indicator(which, cfg)
```

From src/chart/kline/mod.rs (line 222):

```rust
fn make_indicator_with_config(
    which: KlineIndicator,
    cfg: &data::chart::kline::Config,
) -> Box<dyn KlineIndicatorImpl>
// ^ This gets removed; logic merged into make_indicator
```

Call sites to update:

- src/chart/kline/mod.rs:401 — make_indicator_with_config(i, &kline_config)
- src/chart/kline/mod.rs:487 — make_indicator_with_config(i, &kline_config)
- src/chart/kline/mod.rs:581 — make_indicator_with_config(i, &kline_config)
- src/chart/kline/data_ops.rs:56 — make_indicator_with_config(indicator, &self.kline_config)
- src/chart/kline/odb_core.rs:156 — make_indicator_with_config(i, &kline_config)
  </interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate factory functions into single make_indicator</name>
  <files>src/chart/indicator/kline.rs, src/chart/kline/mod.rs, src/chart/kline/data_ops.rs, src/chart/kline/odb_core.rs</files>
  <action>
1. In `src/chart/indicator/kline.rs`:
   - Add import: `use data::chart::kline::Config;` (the Config struct for indicator params)
   - Replace `pub fn make_empty(which: KlineIndicator) -> Box<dyn KlineIndicatorImpl>` with:
     ```rust
     pub fn make_indicator(
         which: KlineIndicator,
         cfg: &data::chart::kline::Config,
     ) -> Box<dyn KlineIndicatorImpl>
     ```
   - Update the match body to be config-aware for 3 indicators:
     - `KlineIndicator::OFI` => `OFIIndicator::with_ema_period(cfg.ofi_ema_period)` (not `::new()`)
     - `KlineIndicator::OFICumulativeEma` => `OFICumulativeEmaIndicator::with_ema_period(cfg.ofi_ema_period)` (not `::new()`)
     - `KlineIndicator::TradeIntensityHeatmap` => `TradeIntensityHeatmapIndicator::with_config(cfg.intensity_lookback, cfg.anomaly_fence)` (not `::new()`)
   - All other arms remain unchanged (they ignore cfg, use `::new()`)
   - Keep the exhaustive match (no wildcard/other arm) -- compiler checks all variants

1. In `src/chart/kline/mod.rs`:
   - Delete the entire `make_indicator_with_config` function (lines ~217-243)
   - Replace all 3 call sites of `make_indicator_with_config(...)` with `indicator::kline::make_indicator(...)`
     - Line ~401: `make_indicator_with_config(i, &kline_config)` => `indicator::kline::make_indicator(i, &kline_config)`
     - Line ~487: same replacement
     - Line ~581: same replacement

2. In `src/chart/kline/data_ops.rs` (line 56):
   - The file currently calls `make_indicator_with_config(indicator, &self.kline_config)` -- this is a call to the function in `super` (kline/mod.rs)
   - Replace with: `crate::chart::indicator::kline::make_indicator(indicator, &self.kline_config)`
   - Or if there's already a `use` for the indicator module, use the shorter path

3. In `src/chart/kline/odb_core.rs` (line 156):
   - Same pattern: replace `make_indicator_with_config(i, &kline_config)` with the new path
   - Use: `crate::chart::indicator::kline::make_indicator(i, &kline_config)`

Note: The `use` imports in data_ops.rs and odb_core.rs may already have `use super::make_indicator_with_config;` or call it via `super::`. Check the actual import style and match it for the new function path.
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -20</automated>
</verify>
<done>Single `make_indicator(which, cfg)` factory exists in `src/chart/indicator/kline.rs`. The old `make_empty` and `make_indicator_with_config` are both gone. All 5 call sites compile against the new function. `cargo clippy -- -D warnings` passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md indicator checklist</name>
  <files>CLAUDE.md</files>
  <action>
Replace the "Adding a New Indicator" section in CLAUDE.md (currently lines ~206-213, a 6-step list) with a verified checklist that reflects the post-Phase-8 reality. The new section should be:

```markdown
### Adding a New Indicator

**Standard subplot indicator (3 files: 2 modified + 1 new):**

1. **`data/src/chart/indicator.rs`** (enum + arrays + Display):
   - Add variant to `KlineIndicator` enum
   - Add to `FOR_SPOT` and/or `FOR_PERPS` arrays (increment array length constant)
   - Add `Display` match arm

2. **`src/chart/indicator/kline.rs`** (factory + module):
   - Add `pub mod <name>;` declaration
   - Add match arm to `make_indicator()` factory

3. **`src/chart/indicator/kline/<name>.rs`** (NEW FILE -- implementation):
   - Implement `KlineIndicatorImpl` trait (see `rsi.rs` or `volume.rs` for reference)

**Extended ceremony (only for configurable or special-rendering indicators):**

- **Configurable params** (e.g., EMA period): also modify `data/src/chart/kline.rs` Config struct + update `make_indicator()` arm to use config
- **Main-canvas rendering** (e.g., heatmap colors candle bodies): also modify `src/chart/kline/mod.rs` draw code

No other files need touching. `EnumMap` derive auto-expands storage. Serde derive handles serialization.
```

Do NOT change any other section of CLAUDE.md. Only replace the "Adding a New Indicator" subsection.
</action>
<verify>
<automated>grep -A 20 "Adding a New Indicator" /Users/terryli/fork-tools/flowsurface/CLAUDE.md | head -25</automated>
</verify>
<done>CLAUDE.md "Adding a New Indicator" section shows verified 3-file checklist with extended ceremony notes. The checklist matches the actual code paths after factory consolidation.</done>
</task>

</tasks>

<verification>
1. `cargo clippy --all-targets -- -D warnings` passes clean
2. `grep -r 'make_empty\|make_indicator_with_config' src/` returns zero hits
3. `grep 'pub fn make_indicator' src/chart/indicator/kline.rs` returns exactly one hit
4. CLAUDE.md contains "3 files" in the indicator section
</verification>

<success_criteria>

- Single consolidated `make_indicator(which, cfg)` factory in `src/chart/indicator/kline.rs`
- Zero references to `make_empty` or `make_indicator_with_config` anywhere in codebase
- CLAUDE.md documents the verified 3-file ceremony as canonical reference
- `cargo clippy -- -D warnings` passes clean
- No behavior changes -- all indicators construct with same parameters as before
  </success_criteria>

<output>
After completion, create `.planning/phases/08-indicator-ceremony-reduction/08-01-SUMMARY.md`
</output>
