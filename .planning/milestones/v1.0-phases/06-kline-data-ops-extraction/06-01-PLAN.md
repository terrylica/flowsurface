---
phase: 06-kline-data-ops-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chart/kline/mod.rs
  - src/chart/kline/data_ops.rs
autonomous: true
requirements:
  - KLINE-01
  - VER-01
  - VER-02
  - VER-03

must_haves:
  truths:
    - "Data insertion methods (insert_hist_klines, insert_open_interest) callable on KlineChart from external code unchanged"
    - "missing_data_task still callable from invalidate() in mod.rs via self.missing_data_task()"
    - "calc_qty_scales still callable from draw() in mod.rs via self.calc_qty_scales(...)"
    - "toggle_indicator still callable from external pane code unchanged"
    - "cargo clippy -- -D warnings passes clean"
    - "No new unsafe code introduced"
  artifacts:
    - path: "src/chart/kline/data_ops.rs"
      provides: "Data insertion and aggregation query methods for KlineChart"
      contains: "impl KlineChart"
      min_lines: 240
    - path: "src/chart/kline/mod.rs"
      provides: "KlineChart struct, rendering, canvas Program impl (reduced LOC)"
      contains: "mod data_ops"
  key_links:
    - from: "src/chart/kline/data_ops.rs"
      to: "src/chart/kline/mod.rs"
      via: "use super::* imports all parent types; impl KlineChart block accesses private fields"
      pattern: "use super::\\*"
    - from: "src/chart/kline/mod.rs"
      to: "src/chart/kline/data_ops.rs"
      via: "mod data_ops declaration makes methods available on self"
      pattern: "mod data_ops"
---

<objective>
Extract 5 data operation methods (~246 LOC) from kline/mod.rs into a new kline/data_ops.rs submodule, reducing mod.rs by ~242 LOC.

Purpose: KLINE-01 -- data flow paths become independently navigable; mod.rs shrinks toward the Phase 7 target of <1800 LOC.
Output: New `src/chart/kline/data_ops.rs` file with 5 methods; updated `src/chart/kline/mod.rs` with `mod data_ops;` declaration and method bodies removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-kline-data-ops-extraction/06-RESEARCH.md

@src/chart/kline/mod.rs
@src/chart/kline/odb_core.rs (first 5 lines -- pattern reference for use super::\*)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data_ops.rs with 5 extracted methods</name>
  <files>src/chart/kline/data_ops.rs</files>
  <read_first>
    - src/chart/kline/mod.rs (FULL file -- must read entirely before any changes)
    - src/chart/kline/odb_core.rs (first 5 lines -- confirm use super::* pattern)
  </read_first>
  <action>
Create `src/chart/kline/data_ops.rs` following the exact `odb_core.rs` submodule pattern:

1. **File header**: Comment explaining purpose (data insertion and aggregation query methods extracted from mod.rs). No `// FILE-SIZE-OK` needed -- this file is small.

2. **Import**: `use super::*;` as the single import line (same as odb_core.rs line 2). This brings all parent module imports into scope.

3. **Check for local imports**: Before copying method bodies, scan each method for local `use` statements or fully-qualified paths not covered by mod.rs top-level imports. Specifically check `missing_data_task` for any `chrono::` or other local imports -- move those inside the method body in data_ops.rs.

4. **impl KlineChart block** with these 5 methods, copied EXACTLY from mod.rs (preserving all comments, formatting, logic):

   a. `pub fn insert_hist_klines(&mut self, req_id: uuid::Uuid, klines_raw: &[Kline])` -- lines 1058-1079
   b. `pub fn insert_open_interest(&mut self, req_id: Option<uuid::Uuid>, oi_data: &[OIData])` -- lines 1081-1094
   c. `pub fn toggle_indicator(&mut self, indicator: KlineIndicator)` -- lines 1590-1618
   d. `pub(super) fn missing_data_task(&mut self) -> Option<Action>` -- lines 655-797. Note: currently `fn` (private) in mod.rs; use `pub(super)` in data_ops.rs so it remains callable from mod.rs methods like `invalidate()`.
   e. `pub(super) fn calc_qty_scales(&self, ...)` -- lines 1096-1133. Same visibility reasoning as missing_data_task. Preserve exact signature (6 parameters: earliest, latest, highest, lowest, step, cluster_kind).

5. **Visibility rules**:
   - `insert_hist_klines`, `insert_open_interest`, `toggle_indicator`: Keep `pub` (called from pane code outside kline module)
   - `missing_data_task`, `calc_qty_scales`: Use `pub(super)` (called only from within kline/mod.rs methods)
     </action>

  <verify>
    <automated>cd /Users/terryli/fork-tools/flowsurface && test -f src/chart/kline/data_ops.rs && grep -c "impl KlineChart" src/chart/kline/data_ops.rs</automated>
  </verify>
  <acceptance_criteria>
    - data_ops.rs exists with `use super::*;` and a single `impl KlineChart` block
    - All 5 methods present with correct visibility (3 pub, 2 pub(super))
    - Method bodies are exact copies from mod.rs (no logic changes)
  </acceptance_criteria>
  <done>data_ops.rs contains all 5 extracted methods with correct visibility and exact code from mod.rs</done>
</task>

<task type="auto">
  <name>Task 2: Update mod.rs -- declare submodule, remove extracted methods, update FILE-SIZE-OK</name>
  <files>src/chart/kline/mod.rs</files>
  <read_first>
    - src/chart/kline/mod.rs (FULL file -- must re-read to confirm exact line numbers before deletion)
    - src/chart/kline/data_ops.rs (verify Task 1 output is correct before removing from mod.rs)
  </read_first>
  <action>
Update `src/chart/kline/mod.rs` with three changes:

1. **Add `mod data_ops;` declaration** after the existing `mod odb_core;` line (around line 40-41). Place it right after the odb_core pub use line, before the `mod rendering;` line. No `pub use` needed -- methods are on KlineChart itself.

2. **Remove the 5 method bodies** from the `impl KlineChart` block. Remove these exact ranges (verify line numbers by re-reading the file first -- they may have shifted):
   - `missing_data_task` method (~lines 655-797)
   - `insert_hist_klines` method (~lines 1058-1079)
   - `insert_open_interest` method (~lines 1081-1094)
   - `calc_qty_scales` method (~lines 1096-1133)
   - `toggle_indicator` method (~lines 1590-1618)

   IMPORTANT: Remove methods from BOTTOM to TOP (toggle_indicator first, then calc_qty_scales, etc.) to avoid line number shifts between deletions.

3. **Update the FILE-SIZE-OK comment** on line 1: Change to reflect reduced LOC. Example: `// FILE-SIZE-OK: upstream file with data ops extracted to data_ops.rs`

4. **Verify no dangling references**: After removal, confirm the remaining `impl KlineChart` block still has proper brace matching. The methods that CALL the extracted methods (e.g., `invalidate()` calls `self.missing_data_task()`, `draw()` calls `self.calc_qty_scales()`) must NOT be modified -- they continue to work because Rust resolves `self.method()` across impl blocks in submodules.
   </action>

  <verify>
    <automated>cd /Users/terryli/fork-tools/flowsurface && mise run lint && cargo test --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mod data_ops;` declared in mod.rs after odb_core
    - All 5 method bodies removed from mod.rs
    - `mise run lint` passes clean (cargo fmt --check + cargo clippy -- -D warnings)
    - `cargo test --workspace` passes (existing tests in mod.rs still work)
    - `grep -r 'unsafe' src/chart/kline/data_ops.rs` returns no hits (VER-03)
    - `wc -l src/chart/kline/mod.rs` shows ~2148 or fewer lines (reduced from 2390)
  </acceptance_criteria>
  <done>mod.rs declares data_ops submodule, all 5 methods removed, lint clean, tests pass, no unsafe code, LOC reduced by ~242</done>
</task>

</tasks>

<verification>
1. `mise run lint` -- clippy clean with -D warnings (VER-01)
2. `cargo test --workspace` -- all existing tests pass (VER-02)
3. `grep -r 'unsafe' src/chart/kline/data_ops.rs` -- no hits (VER-03)
4. `wc -l src/chart/kline/mod.rs` -- below 2200 LOC (KLINE-01 ~242 LOC reduction)
5. `grep 'mod data_ops' src/chart/kline/mod.rs` -- declaration present
6. `grep -c 'pub fn\|pub(super) fn' src/chart/kline/data_ops.rs` -- returns 5
</verification>

<success_criteria>

- data_ops.rs exists with 5 methods (insert_hist_klines, insert_open_interest, toggle_indicator, missing_data_task, calc_qty_scales)
- mod.rs reduced by ~242 LOC (from 2390 to ~2148)
- All callers unchanged -- no external API changes
- Lint clean, tests pass, no unsafe code
  </success_criteria>

<output>
After completion, create `.planning/phases/06-kline-data-ops-extraction/06-01-SUMMARY.md`
</output>
