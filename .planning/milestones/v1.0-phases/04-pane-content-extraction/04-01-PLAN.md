---
phase: 04-pane-content-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screen/dashboard/pane.rs
  - src/screen/dashboard/pane/mod.rs
  - src/screen/dashboard/pane/content.rs
autonomous: true
requirements: [PANE-01, VER-01, VER-02, VER-03]

must_haves:
  truths:
    - "Content enum and all impl blocks live in pane/content.rs, not pane/mod.rs"
    - "All external references (pane::Content) compile without changes"
    - "cargo clippy --all-targets -- -D warnings passes clean"
    - "No unsafe code in the extracted file"
  artifacts:
    - path: "src/screen/dashboard/pane/mod.rs"
      provides: "State struct, enums, view helpers -- the remainder of pane.rs"
      contains: "pub(crate) use content::Content"
    - path: "src/screen/dashboard/pane/content.rs"
      provides: "Content enum, factory methods, helper methods, Display, PartialEq"
      contains: "pub enum Content"
      min_lines: 400
  key_links:
    - from: "src/screen/dashboard/pane/mod.rs"
      to: "src/screen/dashboard/pane/content.rs"
      via: "mod content; pub(crate) use content::Content;"
      pattern: "pub\\(crate\\) use content::Content"
    - from: "src/screen/dashboard.rs"
      to: "src/screen/dashboard/pane/mod.rs"
      via: "pub mod pane (unchanged)"
      pattern: "pane::Content"
---

<objective>
Extract the Content enum and all its methods from pane.rs (~450 LOC) into a new pane/content.rs submodule.

Purpose: pane.rs is a 2431-line god module. This extraction makes Content independently navigable and reduces mod.rs to ~1980 LOC, setting up Phase 5's stream setup extraction.

Output: pane.rs converted to pane/mod.rs + pane/content.rs. All 40+ external references via pane::Content continue working unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pane-content-extraction/04-RESEARCH.md

@src/screen/dashboard/pane.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert pane.rs to pane/mod.rs and extract Content to pane/content.rs</name>
  <files>src/screen/dashboard/pane.rs, src/screen/dashboard/pane/mod.rs, src/screen/dashboard/pane/content.rs</files>
  <read_first>
    - src/screen/dashboard/pane.rs (ENTIRE file -- 2431 lines, read ALL of it before any changes)
    - .planning/phases/04-pane-content-extraction/04-RESEARCH.md (line ranges, visibility changes, imports)
  </read_first>
  <action>
**Step 1: File-to-directory conversion**

Create directory and move file:

```bash
mkdir -p src/screen/dashboard/pane
git mv src/screen/dashboard/pane.rs src/screen/dashboard/pane/mod.rs
```

**Step 2: Create pane/content.rs**

Create `src/screen/dashboard/pane/content.rs` containing ALL of the following extracted from mod.rs:

1. **Content enum definition** (lines 1880-1901 in original):
   - `#[derive(Default)]`
   - `// GitHub Issue: https://github.com/terrylica/opendeviationbar-py/issues/97`
   - `#[allow(clippy::large_enum_variant)]`
   - `pub enum Content` with 6 variants: Starter, Heatmap, Kline, TimeAndSales, Ladder, Comparison

2. **All `impl Content` methods** (lines 1903-2309):
   - `new_heatmap` -- change from `fn` to `pub(super) fn`
   - `new_kline` -- change from `fn` to `pub(super) fn`
   - `placeholder` -- change from `fn` to `pub(super) fn`
   - `last_tick` -- keep `pub fn`
   - `chart_kind` -- keep `pub fn`
   - `toggle_indicator` -- keep `pub fn`
   - `reorder_indicators` -- keep `pub fn`
   - `change_visual_config` -- keep `pub fn`
   - `studies` -- keep `pub fn`
   - `update_studies` -- keep `pub fn`
   - `kind` -- keep `pub fn`
   - `initialized` -- change from `fn` to `pub(super) fn`

3. **Trait impls** (lines 2312-2329):
   - `impl std::fmt::Display for Content`
   - `impl PartialEq for Content`

**Step 3: Add imports to content.rs**

Add these imports at the top of content.rs:

```rust
use crate::chart::{
    comparison::ComparisonChart,
    heatmap::HeatmapChart,
    kline::KlineChart,
};
use crate::screen::dashboard::panel::{ladder::Ladder, timeandsales::TimeAndSales};
use crate::widget::column_drag;
use data::chart::{
    Basis, ViewConfig,
    indicator::{HeatmapIndicator, KlineIndicator, UiIndicator},
};
use data::layout::pane::{ContentKind, Settings, VisualConfig};
use exchange::TickerInfo;
use std::time::Instant;
```

IMPORTANT: Only include imports that content.rs actually uses. Run `cargo clippy` after to catch unused imports. The exact set depends on method bodies -- read each method carefully.

**Step 4: Update mod.rs**

1. Add module declaration and re-export near the top of mod.rs (after the `use` block, before the enum definitions):

```rust
mod content;
pub(crate) use content::Content;
```

1. Delete lines 1880-2329 from mod.rs (the Content enum, all impl Content blocks, Display impl, PartialEq impl). Keep everything else: State, Effect, Status, Action, Message, Event, all State impl blocks, and the free functions (link_group_modal, ticksize_modifier, basis_modifier, by_basis_default).

2. Remove any imports from mod.rs that are now ONLY used by Content methods (not by State or free functions). Let `cargo clippy` guide this -- it will flag unused imports.

**Step 5: Compile check**

Run `cargo clippy --all-targets -- -D warnings`. Fix any issues:

- Missing imports in content.rs: add them
- Unused imports in mod.rs: remove them
- Visibility errors ("method is private"): ensure new_heatmap, new_kline, placeholder, initialized are `pub(super)`
  </action>
  <verify>
  <automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - `test -f src/screen/dashboard/pane/content.rs` succeeds (file exists)
  - `test -f src/screen/dashboard/pane/mod.rs` succeeds (file exists)
  - `test ! -f src/screen/dashboard/pane.rs` succeeds (old file gone)
  - `grep -c 'pub enum Content' src/screen/dashboard/pane/content.rs` returns 1
  - `grep -c 'pub(crate) use content::Content' src/screen/dashboard/pane/mod.rs` returns 1
  - `grep -c 'mod content;' src/screen/dashboard/pane/mod.rs` returns 1
  - `grep -c 'pub(super) fn new_heatmap' src/screen/dashboard/pane/content.rs` returns 1
  - `grep -c 'pub(super) fn new_kline' src/screen/dashboard/pane/content.rs` returns 1
  - `grep -c 'pub(super) fn placeholder' src/screen/dashboard/pane/content.rs` returns 1
  - `grep -c 'pub(super) fn initialized' src/screen/dashboard/pane/content.rs` returns 1
  - `grep -c 'pub enum Content' src/screen/dashboard/pane/mod.rs` returns 0 (enum moved out)
  - `grep -c 'unsafe' src/screen/dashboard/pane/content.rs` returns 0 (VER-03)
  - `cargo clippy --all-targets -- -D warnings` exits 0 (VER-01)
  - `wc -l src/screen/dashboard/pane/content.rs` shows 400+ lines
  - `wc -l src/screen/dashboard/pane/mod.rs` shows ~1980 lines (reduced from 2431)
    </acceptance_criteria>
    <done>
    Content enum, all 12 impl methods, Display and PartialEq impls live in pane/content.rs. mod.rs re-exports Content. All 40+ external pane::Content references compile unchanged. Clippy passes clean. No unsafe code introduced.
    </done>
    </task>

<task type="auto">
  <name>Task 2: Update FILE-SIZE-OK comment and verify final state</name>
  <files>src/screen/dashboard/pane/mod.rs</files>
  <read_first>
    - src/screen/dashboard/pane/mod.rs (line 1-3 only -- check FILE-SIZE-OK comment)
  </read_first>
  <action>
**Step 1: Update FILE-SIZE-OK comment**

The original pane.rs line 1 has:

```rust
// FILE-SIZE-OK: upstream file, splitting out of scope for this fork
```

This comment is now outdated -- we ARE splitting this file. Update line 1 of mod.rs to:

```rust
// FILE-SIZE-OK: upstream pane state machine — Content extracted to content.rs, stream setup next (Phase 5)
```

Preserve line 2 (`// GitHub Issue: https://github.com/flowsurface-rs/flowsurface/pull/89`).

**Step 2: Final verification**

Run the full lint suite:

```bash
mise run lint
```

Verify line counts:

```bash
wc -l src/screen/dashboard/pane/mod.rs src/screen/dashboard/pane/content.rs
```

Expected: mod.rs ~1980 lines, content.rs ~450 lines, total ~2430 (same as original).
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && mise run lint 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - `grep 'FILE-SIZE-OK.*Content extracted' src/screen/dashboard/pane/mod.rs` matches - `mise run lint` exits 0 (fmt check + clippy) - `wc -l src/screen/dashboard/pane/mod.rs` shows value less than 2000
</acceptance_criteria>
<done>
FILE-SIZE-OK comment updated to reflect extraction. Full lint suite passes. mod.rs under 2000 LOC.
</done>
</task>

</tasks>

<verification>
Phase-level verification:

1. `test -f src/screen/dashboard/pane/content.rs` -- PANE-01: Content file exists
2. `grep -c 'pub enum Content' src/screen/dashboard/pane/content.rs` returns 1 -- PANE-01: Content enum in new file
3. `grep -c 'pub enum Content' src/screen/dashboard/pane/mod.rs` returns 0 -- PANE-01: Content removed from mod.rs
4. `cargo clippy --all-targets -- -D warnings` exits 0 -- VER-01
5. `cargo build` exits 0 -- VER-02 (compiles = no behavior change for pure move)
6. `grep -c 'unsafe' src/screen/dashboard/pane/content.rs` returns 0 -- VER-03
7. `wc -l src/screen/dashboard/pane/mod.rs` shows ~1980 -- confirms ~450 LOC reduction
   </verification>

<success_criteria>

- Content enum and all methods in pane/content.rs (~450 LOC)
- pane/mod.rs re-exports Content, reduced to ~1980 LOC
- All 40+ external pane::Content references compile unchanged
- cargo clippy --all-targets -- -D warnings passes clean
- mise run lint passes clean
- No unsafe code introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/04-pane-content-extraction/04-01-SUMMARY.md`
</output>
