---
phase: 03-bool-to-enum-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exchange/src/adapter.rs
  - data/src/conditional_ema.rs
  - data/src/chart/heatmap.rs
  - data/src/panel/ladder.rs
  - src/chart/kline/odb_core.rs
  - src/screen/dashboard.rs
  - src/screen/dashboard/panel/timeandsales.rs
  - src/chart/heatmap.rs
  - src/chart/indicator/kline/ofi_cumulative_ema.rs
  - src/screen/dashboard/panel/ladder.rs
autonomous: true
requirements: [QUAL-01, VER-01, VER-02, VER-03]

must_haves:
  truths:
    - "All 5 identified bool flag functions use enum parameters instead of bare bool"
    - "Every call site reads as a descriptive variant (e.g., Side::Bid) not true/false"
    - "cargo clippy -- -D warnings passes clean with zero new warnings"
    - "No new unsafe code introduced"
    - "No behavior changes -- all enum variants map 1:1 to previous bool values"
  artifacts:
    - path: "exchange/src/adapter.rs"
      provides: "qty_in_quote_value takes SizeUnit instead of bool"
      contains: "unit: SizeUnit"
    - path: "data/src/conditional_ema.rs"
      provides: "EmaAction enum definition + update() takes EmaAction"
      contains: "pub enum EmaAction"
    - path: "data/src/chart/heatmap.rs"
      provides: "OrderRun, CoalescingRun, process_side, update_price_level use Side enum"
      contains: "side: Side"
    - path: "src/chart/kline/odb_core.rs"
      provides: "GapFillProgress enum + insert_raw_trades takes GapFillProgress"
      contains: "pub enum GapFillProgress"
    - path: "data/src/panel/ladder.rs"
      provides: "ChaseTracker::update takes Side instead of is_bid: bool"
      contains: "side: Side"
  key_links:
    - from: "exchange/src/adapter.rs"
      to: "src/screen/dashboard/panel/timeandsales.rs"
      via: "qty_in_quote_value(qty, price, volume_size_unit()) -- SizeUnit passed directly"
      pattern: "volume_size_unit\\(\\)"
    - from: "src/chart/kline/odb_core.rs"
      to: "src/screen/dashboard.rs"
      via: "GapFillProgress propagated from Message::TradesReceived field through dispatch"
      pattern: "GapFillProgress"
    - from: "data/src/panel/ladder.rs"
      to: "data/src/chart/heatmap.rs"
      via: "Shared Side enum imported by heatmap module"
      pattern: "use crate::panel::ladder::Side"
---

<objective>
Replace 5 bool flag arguments with self-documenting enums across the codebase.

Purpose: Call sites read like prose (`Side::Bid`, `GapFillProgress::Complete`) instead of mystery `true`/`false` flags. Addresses QUAL-01.
Output: 5 functions use enum parameters, 2 new enums defined (EmaAction, GapFillProgress), 1 existing enum reused (SizeUnit), 1 existing enum promoted (Side).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bool-to-enum-cleanup/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data crate + exchange crate enum conversions (Targets 1, 2, 3, 5)</name>
  <files>
    exchange/src/adapter.rs,
    data/src/conditional_ema.rs,
    data/src/chart/heatmap.rs,
    data/src/panel/ladder.rs,
    src/screen/dashboard/panel/timeandsales.rs,
    src/chart/heatmap.rs,
    src/chart/indicator/kline/ofi_cumulative_ema.rs,
    src/screen/dashboard/panel/ladder.rs
  </files>
  <read_first>
    exchange/src/adapter.rs,
    exchange/src/unit/qty.rs,
    data/src/conditional_ema.rs,
    data/src/chart/heatmap.rs,
    data/src/panel/ladder.rs,
    src/screen/dashboard/panel/timeandsales.rs,
    src/chart/heatmap.rs,
    src/chart/indicator/kline/ofi_cumulative_ema.rs,
    src/screen/dashboard/panel/ladder.rs
  </read_first>
  <action>
Four independent bool-to-enum conversions. Apply each in order, verifying compilation between them.

**Target 1 -- adapter.rs SizeUnit reuse (QUAL-01):**
In `exchange/src/adapter.rs`, change `qty_in_quote_value` parameter from `size_in_quote_ccy: bool` to `unit: SizeUnit`. Add `use crate::unit::qty::SizeUnit;` if not already imported. Update function body: replace `if size_in_quote_ccy` with `match unit { SizeUnit::Quote => ..., SizeUnit::Base => ... }` (Quote = was true, Base = was false -- Base means multiply by price).

Update 7 call sites. At each site, the pattern is:

```
let size_in_quote_ccy = volume_size_unit() == SizeUnit::Quote;
// ... later ...
market_type.qty_in_quote_value(qty, price, size_in_quote_ccy)
```

Change to pass `volume_size_unit()` directly and remove the intermediate `size_in_quote_ccy` local variable (ONLY if it is not used elsewhere in the same scope -- check each site). Call sites:

- `src/screen/dashboard/panel/timeandsales.rs` (3 sites, lines ~123/237/462)
- `src/chart/heatmap.rs` (2 sites, lines ~497/548/623)
- `data/src/chart/heatmap.rs` (2 sites, lines ~292/477)

IMPORTANT: The `size_in_quote_ccy` variable is ALSO used by `QtyNormalizer::new()` at some of these call sites. Only remove the intermediate variable if `qty_in_quote_value` was its sole consumer in that scope. If `QtyNormalizer::new()` also uses it in the same scope, keep the variable but stop passing it to `qty_in_quote_value`.

**Target 2 -- conditional_ema.rs EmaAction (QUAL-01):**
In `data/src/conditional_ema.rs`, define above the struct:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum EmaAction {
    Update,
    CarryForward,
}
```

Change `update(&mut self, input: f32, active: bool) -> f32` to `update(&mut self, input: f32, action: EmaAction) -> f32`. Replace `if active` with `match action { EmaAction::Update => ..., EmaAction::CarryForward => ... }` (Update = was true, CarryForward = was false).

Update sole call site at `src/chart/indicator/kline/ofi_cumulative_ema.rs` (around line 86): change `self.ema.update(ofi, true)` to `self.ema.update(ofi, EmaAction::Update)`. Add import: `use flowsurface_data::conditional_ema::EmaAction;`.

**Target 3 -- heatmap.rs Side promotion (QUAL-01):**
The `Side` enum already exists at `data/src/panel/ladder.rs:43` as `pub enum Side { Bid, Ask }`. It is already `pub` and the `panel` module is `pub` in `data/src/lib.rs`. Add `#[derive(Debug, Clone, Copy, PartialEq, Eq)]` to `Side` if not already present (check current derives -- it has `Copy, Clone` but may need `Debug, PartialEq, Eq`). Also add `Serialize, Deserialize` derives since it will be used on serialized struct fields (`OrderRun`, `CoalescingRun`).

In `data/src/chart/heatmap.rs`:

- Add import: `use crate::panel::ladder::Side;`
- Change `OrderRun` struct field from `pub is_bid: bool` to `pub side: Side`
- Change `OrderRun::new()` parameter from `is_bid: bool` to `side: Side`
- Change `CoalescingRun` struct field from `pub is_bid: bool` to `pub side: Side`
- Change `HistoricalDepth::process_side()` parameter from `is_bid: bool` to `side: Side`
- Change `HistoricalDepth::update_price_level()` parameter from `is_bid: bool` to `side: Side`
- Update `insert_latest_depth()` calls: `self.process_side(&bids, time, true)` -> `self.process_side(&bids, time, Side::Bid)` and `false` -> `Side::Ask`
- Update all internal comparisons: `if is_bid` or `if self.is_bid` -> `side == Side::Bid` or `self.side == Side::Bid` (or use `side.is_bid()` helper that already exists on `Side`)

In `src/chart/heatmap.rs` (main crate rendering):

- Add import for `Side` (via `flowsurface_data::panel::ladder::Side`)
- Change `depth_color(palette, visual_run.is_bid, alpha)` -> `depth_color(palette, visual_run.side, alpha)` at 3 sites (lines ~533, 564, 590)
- Change `depth_color` function signature from `is_bid: bool` to `side: Side`. Update body: `if is_bid` -> `if side.is_bid()`
- Update any other reads of `.is_bid` on `OrderRun`/`CoalescingRun` to use `.side` field + `.is_bid()` method if bool needed, or match on `Side::Bid`/`Side::Ask`

NOTE on serde: `OrderRun` and `CoalescingRun` are runtime depth data (ephemeral, rebuilt from WebSocket on each session), NOT persisted in saved-state.json. No backward compatibility concern.

**Target 5 -- ladder.rs ChaseTracker Side reuse (QUAL-01):**
In `data/src/panel/ladder.rs`, change `ChaseTracker::update()` from `is_bid: bool` to `side: Side`. Update body: replace `if is_bid { Direction::Up } else { Direction::Down }` with `match side { Side::Bid => Direction::Up, Side::Ask => Direction::Down }`.

Update 2 call sites in `src/screen/dashboard/panel/ladder.rs`:

- `.update(raw_best_bid, true, ...)` -> `.update(raw_best_bid, Side::Bid, ...)`
- `.update(raw_best_ask, false, ...)` -> `.update(raw_best_ask, Side::Ask, ...)`
  Add import for `Side` from `flowsurface_data::panel::ladder::Side`.
  </action>
  <verify>
  <automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
  - grep -n "unit: SizeUnit" exchange/src/adapter.rs shows the updated parameter
  - grep -n "pub enum EmaAction" data/src/conditional_ema.rs shows the new enum
  - grep -n "side: Side" data/src/chart/heatmap.rs shows updated struct fields (OrderRun + CoalescingRun)
  - grep -n "side: Side" data/src/panel/ladder.rs shows updated ChaseTracker::update parameter
  - grep "is_bid.\*bool" data/src/chart/heatmap.rs returns zero hits (all bool fields replaced)
  - grep "is_bid.\*bool" data/src/panel/ladder.rs | grep -v "fn is_bid" returns zero hits (only the helper method remains)
  - cargo clippy --all-targets -- -D warnings passes clean
    </acceptance_criteria>
    <done>
    Targets 1, 2, 3, and 5 converted: qty_in_quote_value takes SizeUnit, ConditionalEma::update takes EmaAction, heatmap OrderRun/CoalescingRun/process_side/update_price_level use Side, ChaseTracker::update takes Side. All call sites updated. clippy clean.
    </done>
    </task>

<task type="auto">
  <name>Task 2: ODB gap-fill enum conversion + Message variant propagation (Target 4)</name>
  <files>
    src/chart/kline/odb_core.rs,
    src/screen/dashboard.rs
  </files>
  <read_first>
    src/chart/kline/odb_core.rs,
    src/screen/dashboard.rs
  </read_first>
  <action>
**Target 4 -- odb_core.rs GapFillProgress (QUAL-01):**

In `src/chart/kline/odb_core.rs`, define near the top of the file (or near `insert_raw_trades`):

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum GapFillProgress {
    Streaming,
    Complete,
}
```

Change `insert_raw_trades(&mut self, raw_trades: Vec<Trade>, is_batches_done: bool)` to `insert_raw_trades(&mut self, raw_trades: Vec<Trade>, progress: GapFillProgress)`. Update function body: replace `if is_batches_done` with `if progress == GapFillProgress::Complete` (Complete = was true, Streaming = was false).

In `src/screen/dashboard.rs`, propagate the enum through the message chain:

- Find the `Message` enum variant that carries `is_batches_done: bool` (around line 1102). Change field to `progress: GapFillProgress` (or `gap_fill_progress: GapFillProgress`).
- Add import: `use crate::chart::kline::odb_core::GapFillProgress;`
- Update the dispatch code (around line 1124) where `c.insert_raw_trades(trades.to_owned(), is_batches_done)` becomes `c.insert_raw_trades(trades.to_owned(), progress)`.
- Update the conditional check (around line 1126) where `if is_batches_done` becomes `if progress == GapFillProgress::Complete`.
- Trace the Message variant construction site (where `is_batches_done` is set) -- likely in a fetcher or connector. Update that site too: `is_batches_done: true` -> `progress: GapFillProgress::Complete`, `is_batches_done: false` -> `progress: GapFillProgress::Streaming`. Search for all sites that construct this Message variant.

Run `cargo clippy --all-targets -- -D warnings` to verify.
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - grep -n "pub enum GapFillProgress" src/chart/kline/odb_core.rs shows the new enum - grep -n "progress: GapFillProgress" src/chart/kline/odb_core.rs shows the updated parameter - grep "is_batches_done" src/chart/kline/odb_core.rs returns zero hits - grep "is_batches_done" src/screen/dashboard.rs returns zero hits - cargo clippy --all-targets -- -D warnings passes clean
</acceptance_criteria>
<done>
Target 4 converted: insert_raw_trades takes GapFillProgress enum, Message::TradesReceived variant propagates the enum, all construction and dispatch sites updated. clippy clean. No bool flags remain in the 5 identified functions.
</done>
</task>

</tasks>

<verification>
Final phase verification:

1. `cargo clippy --all-targets -- -D warnings` passes clean (VER-01)
2. `grep -rn "is_batches_done\|size_in_quote_ccy: bool\|active: bool\|is_bid: bool" exchange/src/adapter.rs data/src/conditional_ema.rs data/src/chart/heatmap.rs src/chart/kline/odb_core.rs data/src/panel/ladder.rs` returns zero hits for the 5 target functions (QUAL-01)
3. `grep -rn "unsafe" exchange/src/adapter.rs data/src/conditional_ema.rs data/src/chart/heatmap.rs src/chart/kline/odb_core.rs data/src/panel/ladder.rs src/screen/dashboard.rs` shows no new unsafe code (VER-03)
4. No behavioral change -- all enum variants map 1:1 to previous bool values (VER-02, verified by type system)
   </verification>

<success_criteria>

- All 5 bool flag functions use enum parameters
- 2 new enums defined: EmaAction (conditional_ema.rs), GapFillProgress (odb_core.rs)
- 1 enum reused directly: SizeUnit (already existed in qty.rs)
- 1 enum promoted to shared use: Side (ladder.rs, now also used by heatmap.rs)
- cargo clippy -- -D warnings passes clean
- Zero remaining bool flag parameters in the 5 identified target functions
  </success_criteria>

<output>
After completion, create `.planning/phases/03-bool-to-enum-cleanup/03-01-SUMMARY.md`
</output>
