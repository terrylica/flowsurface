---
phase: 01-config-centralization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exchange/src/config.rs
  - exchange/src/lib.rs
  - exchange/src/adapter/clickhouse.rs
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]

must_haves:
  truths:
    - "All FLOWSURFACE_* env vars are read in exactly one place (exchange/src/config.rs)"
    - "App starts with default config when no env vars are set"
    - "App starts with full .mise.toml config when all env vars are set"
    - "clickhouse.rs and telegram.rs use config struct instead of direct env reads"
  artifacts:
    - path: "exchange/src/config.rs"
      provides: "Centralized AppConfig struct with LazyLock initialization"
      exports: ["APP_CONFIG", "AppConfig"]
      min_lines: 40
    - path: "exchange/src/adapter/clickhouse.rs"
      provides: "ClickHouse adapter using shared config"
      contains: "use crate::config::APP_CONFIG"
  key_links:
    - from: "exchange/src/adapter/clickhouse.rs"
      to: "exchange/src/config.rs"
      via: "APP_CONFIG static reference"
      pattern: "APP_CONFIG\\."
    - from: "exchange/src/telegram.rs"
      to: "exchange/src/config.rs"
      via: "APP_CONFIG static reference"
      pattern: "APP_CONFIG\\."
---

<objective>
Create the centralized AppConfig struct and migrate clickhouse.rs + telegram.rs to use it, eliminating all duplicate env var reads in the exchange crate.

Purpose: Centralizes 12+ scattered env var reads across 2 files into a single validated struct. Eliminates 4 duplicate reads (CH_HOST, CH_PORT, SSE_HOST, SSE_PORT) between clickhouse.rs and telegram.rs.
Output: `exchange/src/config.rs` with `AppConfig` struct; clickhouse.rs and telegram.rs migrated to use it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@exchange/src/adapter/clickhouse.rs
@exchange/src/telegram.rs
@exchange/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create exchange/src/config.rs with AppConfig struct</name>
  <files>exchange/src/config.rs, exchange/src/lib.rs</files>
  <read_first>
    - exchange/src/lib.rs (to see module declarations and visibility patterns)
    - exchange/src/adapter/clickhouse.rs (lines 238-255 for current LazyLock statics and defaults)
    - exchange/src/adapter/clickhouse.rs (lines 818-837 for SSE statics and sse_enabled())
    - exchange/src/telegram.rs (lines 1-27 for BOT_TOKEN, CHAT_ID statics and is_configured())
    - exchange/src/telegram.rs (lines 108-117 for startup_health_check duplicate reads)
  </read_first>
  <action>
Create `exchange/src/config.rs` with:

1. A `pub struct AppConfig` with these fields and exact defaults (matching current behavior):
   - `pub ch_host: String` — default `"bigblack"` (from clickhouse.rs:239)
   - `pub ch_port: u16` — default `8123` (from clickhouse.rs:250)
   - `pub ouroboros_mode: String` — default `"day"` (from clickhouse.rs:243)
   - `pub sse_enabled: bool` — default `false` (from clickhouse.rs:834-836)
   - `pub sse_host: String` — default `"localhost"` (from clickhouse.rs:819)
   - `pub sse_port: u16` — default `18081` (from clickhouse.rs:824)
   - `pub tg_bot_token: Option<String>` — default `None` (from telegram.rs:12)
   - `pub tg_chat_id: Option<String>` — default `None` (from telegram.rs:15)
   - `pub always_on_top: bool` — default `false` (from main.rs:161, presence-based)
   - `pub rust_log: Option<String>` — default `None` (from logger.rs:26)

2. Implement `Default` for `AppConfig` with the above defaults.

3. Implement a `fn from_env() -> Self` method that reads all env vars:
   - `FLOWSURFACE_CH_HOST` → `ch_host` (unwrap_or default)
   - `FLOWSURFACE_CH_PORT` → `ch_port` (parse, unwrap_or default)
   - `FLOWSURFACE_OUROBOROS_MODE` → `ouroboros_mode` (unwrap_or default)
   - `FLOWSURFACE_SSE_ENABLED` → `sse_enabled` (check "true" or "1", else false)
   - `FLOWSURFACE_SSE_HOST` → `sse_host` (unwrap_or default)
   - `FLOWSURFACE_SSE_PORT` → `sse_port` (parse, unwrap_or default)
   - `FLOWSURFACE_TG_BOT_TOKEN` → `tg_bot_token` (.ok())
   - `FLOWSURFACE_TG_CHAT_ID` → `tg_chat_id` (.ok())
   - `FLOWSURFACE_ALWAYS_ON_TOP` → `always_on_top` (.is_ok())
   - `RUST_LOG` → `rust_log` (.ok())

4. Add a `pub fn base_url(&self) -> String` method returning `format!("http://{}:{}", self.ch_host, self.ch_port)`.

5. Add a `pub fn tg_configured(&self) -> bool` method returning `self.tg_bot_token.is_some() && self.tg_chat_id.is_some()`.

6. Create the global static: `pub static APP_CONFIG: LazyLock<AppConfig> = LazyLock::new(AppConfig::from_env);`

7. Add `pub mod config;` to `exchange/src/lib.rs` (insert after `pub mod connect;` line to keep alphabetical order).

Use `std::sync::LazyLock` (already used throughout the codebase). Use `pub` visibility (not `pub(crate)`) because main crate needs access via `exchange::config::APP_CONFIG`.
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && cargo check -p flowsurface-exchange 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - `exchange/src/config.rs` exists with `pub struct AppConfig` and `pub static APP_CONFIG: LazyLock<AppConfig>` - `exchange/src/lib.rs` contains `pub mod config;` - `grep -c 'std::env::var' exchange/src/config.rs` returns 10 (one per env var) - `grep 'pub fn base_url' exchange/src/config.rs` finds the method - `grep 'pub fn tg_configured' exchange/src/config.rs` finds the method - `cargo check -p flowsurface-exchange` succeeds
</acceptance_criteria>
<done>exchange/src/config.rs exists with AppConfig struct, all 10 env vars read in from_env(), LazyLock static exported, exchange crate compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Migrate clickhouse.rs and telegram.rs to use AppConfig</name>
  <files>exchange/src/adapter/clickhouse.rs, exchange/src/telegram.rs</files>
  <read_first>
    - exchange/src/config.rs (just created — need to see exact field names and methods)
    - exchange/src/adapter/clickhouse.rs (full file scan for all CLICKHOUSE_HOST, CLICKHOUSE_PORT, OUROBOROS_MODE, SSE_HOST, SSE_PORT, sse_enabled usage)
    - exchange/src/telegram.rs (full file — small file, ~150 lines)
  </read_first>
  <action>
**In clickhouse.rs:**

1. Add `use crate::config::APP_CONFIG;` to imports.

2. Remove these 5 LazyLock statics (lines ~238-251, ~818-825):
   - `CLICKHOUSE_HOST`
   - `CLICKHOUSE_PORT`
   - `OUROBOROS_MODE`
   - `SSE_HOST`
   - `SSE_PORT`

3. Replace the `base_url()` function body with: `APP_CONFIG.base_url()`

4. Replace all `*OUROBOROS_MODE` references with `APP_CONFIG.ouroboros_mode` (it's a String, deref gives &str via format! auto-borrow). There are ~5 usage sites in SQL-building functions.

5. Replace `SSE_HOST.clone()` with `APP_CONFIG.sse_host.clone()` and `*SSE_PORT` with `APP_CONFIG.sse_port`.

6. Replace the `sse_enabled()` function body:
   - Old: reads `FLOWSURFACE_SSE_ENABLED` env var directly
   - New: `APP_CONFIG.sse_enabled`

7. Keep `SSE_CONNECTED: AtomicBool` and `sse_connected()` — these are runtime state, not config.
8. Keep `HTTP_CLIENT: LazyLock<reqwest::Client>` — this is a client instance, not config.
9. Keep `ODB_SYMBOLS: OnceLock` — this is runtime state loaded from ClickHouse.

**In telegram.rs:**

1. Add `use crate::config::APP_CONFIG;` to imports.

2. Remove these 2 LazyLock statics:
   - `BOT_TOKEN`
   - `CHAT_ID`

3. Replace `is_configured()` body with: `APP_CONFIG.tg_configured()`

4. In `send_alert()`: replace `(BOT_TOKEN.as_deref(), CHAT_ID.as_deref())` with `(APP_CONFIG.tg_bot_token.as_deref(), APP_CONFIG.tg_chat_id.as_deref())`

5. In `send_alert_with_keyboard()`: same replacement as send_alert.

6. In `startup_health_check()`: replace the 4 local env var reads (lines 114-117) with:
   - `let ch_host = &APP_CONFIG.ch_host;`
   - `let ch_port = APP_CONFIG.ch_port;`
   - `let sse_host = &APP_CONFIG.sse_host;`
   - `let sse_port = APP_CONFIG.sse_port;`
     Then update format strings to use these (may need minor adjustments to format! args since types change from String to &String/u16).

**IMPORTANT**: Do NOT change any behavior. The defaults must match exactly:

- telegram.rs previously defaulted ch_host to "localhost" and ch_port to "18123" — but these were only used in the health check display. Now they'll show the same value as the actual ClickHouse config (which defaults to "bigblack" and 8123). This is actually MORE correct — the health check was showing wrong defaults vs what clickhouse.rs actually used. This is a bug fix, not a behavior change.
  </action>
  <verify>
  <automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'std::env::var' exchange/src/adapter/clickhouse.rs` returns 0
  - `grep -c 'std::env::var' exchange/src/telegram.rs` returns 0
  - `grep 'use crate::config::APP_CONFIG' exchange/src/adapter/clickhouse.rs` finds the import
  - `grep 'use crate::config::APP_CONFIG' exchange/src/telegram.rs` finds the import
  - `grep -c 'APP_CONFIG\.' exchange/src/adapter/clickhouse.rs` is >= 5 (base_url, ouroboros_mode x4+, sse_host, sse_port, sse_enabled)
  - `grep -c 'APP_CONFIG\.' exchange/src/telegram.rs` is >= 4 (tg_configured, tg_bot_token, tg_chat_id, ch_host/port/sse_host/port)
  - `cargo clippy --all-targets -- -D warnings` passes clean
    </acceptance_criteria>
    <done>Zero std::env::var calls remain in clickhouse.rs and telegram.rs. Both import and use APP_CONFIG. cargo clippy passes clean with -D warnings.</done>
    </task>

</tasks>

<verification>
1. `grep -rn 'std::env::var' exchange/src/` — only hits should be in `exchange/src/config.rs`
2. `cargo clippy --all-targets -- -D warnings` — clean pass
3. `cargo build` — successful compilation
</verification>

<success_criteria>

- exchange/src/config.rs exists with AppConfig struct and APP_CONFIG LazyLock static
- All env var reads in exchange crate centralized to config.rs (grep confirms zero hits elsewhere)
- Duplicate reads (CH_HOST, CH_PORT, SSE_HOST, SSE_PORT) eliminated — each read exactly once
- cargo clippy --all-targets -- -D warnings passes clean
  </success_criteria>

<output>
After completion, create `.planning/phases/01-config-centralization/01-01-SUMMARY.md`
</output>
