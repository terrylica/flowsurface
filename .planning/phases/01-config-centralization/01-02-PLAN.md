---
phase: 01-config-centralization
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/main.rs
  - src/logger.rs
autonomous: true
requirements: [VER-01, VER-02, VER-03]

must_haves:
  truths:
    - "grep -r 'std::env::var' src/ exchange/src/ returns zero hits outside exchange/src/config.rs"
    - "cargo clippy --all-targets -- -D warnings passes clean"
    - "No unsafe code introduced"
    - "App behavior unchanged — same window level, same log level selection"
  artifacts:
    - path: "src/main.rs"
      provides: "Main app using AppConfig for ALWAYS_ON_TOP"
      contains: "exchange::config::APP_CONFIG"
    - path: "src/logger.rs"
      provides: "Logger using AppConfig for RUST_LOG"
      contains: "exchange::config::APP_CONFIG"
  key_links:
    - from: "src/main.rs"
      to: "exchange/src/config.rs"
      via: "exchange::config::APP_CONFIG import"
      pattern: "APP_CONFIG\\.always_on_top"
    - from: "src/logger.rs"
      to: "exchange/src/config.rs"
      via: "exchange::config::APP_CONFIG import"
      pattern: "APP_CONFIG\\.rust_log"
---

<objective>
Migrate the main crate's env var reads (main.rs, logger.rs) to use the centralized AppConfig, then verify the entire codebase has zero scattered env reads.

Purpose: Completes the config centralization by eliminating the last 3 env var reads outside config.rs. Verifies all VER requirements (clippy clean, no behavior change, no unsafe).
Output: Clean codebase with all env vars flowing through AppConfig.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-centralization/01-01-SUMMARY.md
@src/main.rs
@src/logger.rs

<interfaces>
<!-- From exchange/src/config.rs (created in Plan 01) -->
```rust
pub struct AppConfig {
    pub ch_host: String,
    pub ch_port: u16,
    pub ouroboros_mode: String,
    pub sse_enabled: bool,
    pub sse_host: String,
    pub sse_port: u16,
    pub tg_bot_token: Option<String>,
    pub tg_chat_id: Option<String>,
    pub always_on_top: bool,
    pub rust_log: Option<String>,
}

pub static APP_CONFIG: LazyLock<AppConfig> = LazyLock::new(AppConfig::from_env);

````
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate main.rs and logger.rs to use AppConfig</name>
  <files>src/main.rs, src/logger.rs</files>
  <read_first>
    - exchange/src/config.rs (verify exact field names and static name from Plan 01)
    - src/main.rs (lines 155-170 for ALWAYS_ON_TOP window level usage)
    - src/main.rs (lines 210-220 for ALWAYS_ON_TOP set_on_top task usage)
    - src/logger.rs (lines 20-35 for RUST_LOG usage)
  </read_first>
  <action>
**In src/main.rs:**

1. Add `use exchange::config::APP_CONFIG;` to imports (near existing `use exchange::...` imports).

2. Replace line ~161: `if std::env::var("FLOWSURFACE_ALWAYS_ON_TOP").is_ok()` with `if APP_CONFIG.always_on_top`

3. Replace line ~213: `if std::env::var("FLOWSURFACE_ALWAYS_ON_TOP").is_ok()` with `if APP_CONFIG.always_on_top`

4. Remove `use std::env;` if it was only used for env::var (check first — it may be used for other things).

**In src/logger.rs:**

1. Add `use exchange::config::APP_CONFIG;` to imports.

2. Replace lines ~26-32:
   ```rust
   // Old:
   let level_filter = std::env::var("RUST_LOG")
       .ok()
       .as_deref()
       .map(str::parse::<log::Level>)
       .transpose()?
       .unwrap_or(default_level)
       .to_level_filter();

   // New:
   let level_filter = APP_CONFIG.rust_log
       .as_deref()
       .map(str::parse::<log::Level>)
       .transpose()?
       .unwrap_or(default_level)
       .to_level_filter();
````

**Behavior preservation**: Both replacements produce identical runtime behavior:

- `ALWAYS_ON_TOP`: `std::env::var().is_ok()` returns true when var exists (any value). `APP_CONFIG.always_on_top` does the same via `from_env()`.
- `RUST_LOG`: `std::env::var().ok()` returns `Option<String>`. `APP_CONFIG.rust_log` stores the same `Option<String>`.
  </action>
  <verify>
  <automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
  - `grep -c 'std::env::var' src/main.rs` returns 0
  - `grep -c 'std::env::var' src/logger.rs` returns 0
  - `grep 'APP_CONFIG.always_on_top' src/main.rs` finds 2 hits
  - `grep 'APP_CONFIG.rust_log' src/logger.rs` finds 1 hit
  - `cargo clippy --all-targets -- -D warnings` passes clean
    </acceptance_criteria>
    <done>Zero std::env::var calls remain in src/main.rs and src/logger.rs. Both use APP_CONFIG fields. clippy passes clean.</done>
    </task>

<task type="auto">
  <name>Task 2: Final verification — zero scattered env reads</name>
  <files></files>
  <read_first>
    - exchange/src/config.rs (confirm it has all env var reads)
  </read_first>
  <action>
Run the full verification suite to confirm the phase goal is met:

1. `grep -rn 'std::env::var' src/ exchange/src/` — MUST return hits ONLY in `exchange/src/config.rs`
2. `cargo clippy --all-targets -- -D warnings` — MUST pass clean
3. `grep -rn 'unsafe' exchange/src/config.rs src/main.rs src/logger.rs` — MUST return 0 hits (no new unsafe code)
4. `cargo build --release 2>&1 | tail -5` — MUST compile successfully

If any check fails, fix the issue before proceeding. This is the phase gate.

NOTE: `data/src/lib.rs` (FLOWSURFACE_DATA_PATH) and `data/src/telemetry.rs` (FLOWSURFACE_TELEMETRY) are intentionally OUT OF SCOPE. These are in the data crate and not covered by the roadmap success criteria grep pattern (`src/ exchange/src/`). They can be migrated in a future phase if desired.
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && grep -rn 'std::env::var' src/ exchange/src/ | grep -v 'exchange/src/config.rs' | wc -l | tr -d ' '</automated>
</verify>
<acceptance_criteria> - `grep -rn 'std::env::var' src/ exchange/src/ | grep -v 'exchange/src/config.rs' | wc -l` returns 0 - `cargo clippy --all-targets -- -D warnings` exits 0 - `grep -rn 'unsafe' exchange/src/config.rs src/main.rs src/logger.rs exchange/src/adapter/clickhouse.rs exchange/src/telegram.rs | wc -l` returns 0 - `cargo build --release` succeeds
</acceptance_criteria>
<done>Phase 1 success criteria fully met: single AppConfig struct, zero scattered env reads in src/ and exchange/src/, clippy clean, no unsafe, no behavior changes.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'std::env::var' src/ exchange/src/ | grep -v config.rs` — zero lines
2. `cargo clippy --all-targets -- -D warnings` — clean
3. `cargo build --release` — succeeds
4. Roadmap success criterion: "A single AppConfig struct exists with all env vars, initialized via LazyLock" — confirmed
</verification>

<success_criteria>

- Zero std::env::var hits outside exchange/src/config.rs in both src/ and exchange/src/
- cargo clippy --all-targets -- -D warnings passes clean
- No new unsafe code anywhere in modified files
- Release build compiles successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/01-config-centralization/01-02-SUMMARY.md`
</output>
