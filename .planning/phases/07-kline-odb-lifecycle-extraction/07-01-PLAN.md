---
phase: 07-kline-odb-lifecycle-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chart/kline/odb_lifecycle.rs
  - src/chart/kline/mod.rs
autonomous: true
requirements: [KLINE-02, KLINE-03, VER-01, VER-02, VER-03]

must_haves:
  truths:
    - "ODB lifecycle methods (watchdog, sentinel, viewport digest, telemetry snapshot) are callable from mod.rs invalidate()"
    - "kline/mod.rs is below 1800 LOC"
    - "All existing kline tests pass unchanged"
    - "cargo clippy -- -D warnings passes clean"
    - "No new unsafe code introduced"
  artifacts:
    - path: "src/chart/kline/odb_lifecycle.rs"
      provides: "ODB lifecycle orchestration methods"
      contains: "pub(super) fn check_trade_feed_watchdog"
    - path: "src/chart/kline/mod.rs"
      provides: "KlineChart struct with delegated lifecycle calls"
      contains: "mod odb_lifecycle"
  key_links:
    - from: "src/chart/kline/mod.rs"
      to: "src/chart/kline/odb_lifecycle.rs"
      via: "self.check_trade_feed_watchdog() call in invalidate()"
      pattern: "self\\.check_trade_feed_watchdog\\(\\)"
    - from: "src/chart/kline/mod.rs"
      to: "src/chart/kline/odb_lifecycle.rs"
      via: "self.run_sentinel_audit(t) call in invalidate()"
      pattern: "self\\.run_sentinel_audit\\("
    - from: "src/chart/kline/odb_lifecycle.rs"
      to: "src/chart/kline/odb_core.rs"
      via: "self.audit_bar_continuity() cross-submodule call"
      pattern: "self\\.audit_bar_continuity\\(\\)"
---

<objective>
Extract 4 ODB lifecycle orchestration blocks from kline/mod.rs invalidate() into kline/odb_lifecycle.rs, plus relocate lifecycle-related tests.

Purpose: Isolate fork-specific ODB complexity (watchdog, sentinel, viewport digest, telemetry) into a dedicated submodule, bringing kline/mod.rs below 1800 LOC.
Output: New odb_lifecycle.rs submodule (~400 LOC), mod.rs reduced to ~1736-1796 LOC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-kline-odb-lifecycle-extraction/07-RESEARCH.md
@.planning/phases/06-kline-data-ops-extraction/06-01-SUMMARY.md

@src/chart/kline/mod.rs
@src/chart/kline/odb_core.rs
@src/chart/kline/data_ops.rs

<interfaces>
<!-- Established extraction pattern from odb_core.rs and data_ops.rs -->

From src/chart/kline/odb_core.rs (line 1-2):

```rust
// FILE-SIZE-OK: ODB core logic is tightly coupled — CH reconciliation, gap-fill, trade insertion, SSE
use super::*;
```

From src/chart/kline/data_ops.rs (line 1-4):

```rust
// Data insertion and aggregation query methods extracted from kline/mod.rs.
use super::*;

impl KlineChart {
```

Pattern: `use super::*` imports all types from mod.rs. Methods are `pub(super)` for internal-only, `pub` for cross-module callers. All methods are `impl KlineChart` blocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create odb_lifecycle.rs with 4 extracted lifecycle methods + relocated tests</name>
  <read_first>src/chart/kline/mod.rs (FULL FILE — must read all 2160 lines before any changes)</read_first>
  <files>src/chart/kline/odb_lifecycle.rs</files>
  <action>
Create `src/chart/kline/odb_lifecycle.rs` following the exact pattern from odb_core.rs and data_ops.rs:

1. **File header**: Comment explaining purpose, then `use super::*;`

2. **Extract 4 methods into `impl KlineChart` block**, each `pub(super)`:

   a. `check_trade_feed_watchdog(&mut self)` — lines 1025-1058 of mod.rs. Move ALL guard logic (the `if self.last_trade_received_ms > 0` outer check) INTO the method so the call site is just `self.check_trade_feed_watchdog();`. Include the SystemTime import via `use std::time::{SystemTime, UNIX_EPOCH};` at the top of the method body or file-level (super::\* should cover most imports, but SystemTime may need explicit import — check if it's already in mod.rs imports).

   b. `run_sentinel_audit(&mut self, now: std::time::Instant)` — lines 1060-1240 of mod.rs. Move ALL guard conditions into the method body: the `is_odb()` check, the `!fetching_trades.0` check, the `duration_since >= 60s` check. If guards fail, return early. The method body is the full sentinel block including anomaly classification, Telegram alerts, refetch triggering. This method calls `self.audit_bar_continuity()` which lives in odb_core.rs — this works because both are `impl KlineChart` and `audit_bar_continuity` is `pub(super)`.

   c. `emit_viewport_digest(&mut self, now: std::time::Instant)` — lines 1242-1323 of mod.rs. Move ALL guard conditions into the method body: `is_odb()` check, `duration_since >= 60s` check, `TickBased` match, `!datapoints.is_empty()` check.

   d. `emit_telemetry_snapshot(&mut self, now: std::time::Instant)` — lines 1325-1370 of mod.rs. Gate with `#[cfg(feature = "telemetry")]` on the method definition. Move ALL guard conditions into the method: `is_odb()` check, `duration_since >= 30s` check.

3. **Relocate lifecycle-related tests** to a `#[cfg(test)] mod tests` block at the bottom of odb_lifecycle.rs:
   - The `cooldown_arithmetic_*` tests (3 tests, lines 1994-2013)
   - The `guard_allows` helper function and its 5 tests (lines 2061-2143)
   - Update imports: `use data::chart::Basis;` and `use exchange::Timeframe;` (adjust paths since `super::` now resolves to the odb_lifecycle module — use `super::super::*` is NOT the pattern; instead use crate-level imports like the existing tests do: `use data::chart::Basis;` and `use exchange::Timeframe;`)

CRITICAL: This is an exact code copy — no logic changes. Every line of the extracted code must be identical to the source. Only the method signature wrapper and early-return guards are new code.
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && test -f src/chart/kline/odb_lifecycle.rs && echo "file exists"</automated>
</verify>
<acceptance_criteria> - odb_lifecycle.rs exists with 4 pub(super) methods and relocated tests - File follows use super::\* pattern from odb_core.rs - emit_telemetry_snapshot has #[cfg(feature = "telemetry")] gate - Code is exact copy of source blocks (no logic changes)
</acceptance_criteria>
<done>odb_lifecycle.rs contains all 4 lifecycle methods + 8 relocated tests</done>
</task>

<task type="auto">
  <name>Task 2: Update mod.rs — declare submodule, replace inline blocks with method calls, remove relocated tests</name>
  <read_first>src/chart/kline/mod.rs (FULL FILE — re-read after Task 1 to confirm current state)</read_first>
  <files>src/chart/kline/mod.rs</files>
  <action>
Update `src/chart/kline/mod.rs`:

1. **Add submodule declaration** near the top, alongside existing `mod odb_core;` and `mod data_ops;`:

   ```rust
   mod odb_lifecycle;
   ```

2. **Replace the 4 inline blocks in invalidate()** with method calls. The section between line 1024 (end of cache clearing) and line 1371 (before `if let Some(t) = now { self.last_tick = t; ... }`) should become:

   ```rust
   self.check_trade_feed_watchdog();

   if let Some(t) = now {
       self.run_sentinel_audit(t);
       self.emit_viewport_digest(t);
   }

   #[cfg(feature = "telemetry")]
   if let Some(t) = now {
       self.emit_telemetry_snapshot(t);
   }
   ```

   This replaces ~343 LOC (lines 1025-1370) with ~10 lines.

3. **Remove relocated tests** from the `#[cfg(test)] mod tests` block:
   - Remove `cooldown_arithmetic_blocks_within_30s`, `cooldown_arithmetic_allows_after_30s`, `cooldown_arithmetic_exact_boundary` (3 tests)
   - Remove `guard_allows` helper function and all 5 guard tests: `guard_composition_all_false`, `guard_fetching_blocks`, `guard_already_requested_blocks`, `guard_cooldown_blocks`, `guard_non_odb_blocks`, `guard_all_clear_allows`
   - Keep: `is_gap`, `make_trade`, `gap_of_one_is_not_a_gap`, `gap_of_two_is_a_gap`, `saturating_sub_handles_reorder`, `make_trade_has_correct_id`, `gap_fill_request_fields`, `dedup_fence_filters_stale_trades`, `dedup_fence_none_passes_all`, `gap_detection_skipped_for_gap_fill_trades`

4. **Update the FILE-SIZE-OK comment** on line 1 to reflect the new extraction:

   ```rust
   // FILE-SIZE-OK: upstream file with data ops in data_ops.rs, ODB lifecycle in odb_lifecycle.rs
   ```

5. **Verify the tail of invalidate()** is preserved — the `if let Some(t) = now { self.last_tick = t; self.missing_data_task() }` block must remain as the method's return expression.

CRITICAL: The `invalidate()` method returns `Option<Action>` — the tail `self.missing_data_task()` must remain the final expression. The extracted method calls are fire-and-forget (no return values).
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 && cargo test -p flowsurface --lib -- chart::kline 2>&1 | tail -10 && echo "--- LOC count ---" && wc -l src/chart/kline/mod.rs && echo "--- unsafe check ---" && grep -c "unsafe" src/chart/kline/odb_lifecycle.rs || echo "0 unsafe"</automated>
</verify>
<acceptance_criteria> - cargo clippy -- -D warnings passes clean - All kline tests pass (both mod.rs and odb_lifecycle.rs tests) - wc -l mod.rs shows below 1800 LOC - No unsafe in odb_lifecycle.rs - invalidate() return chain preserved (missing_data_task at tail)
</acceptance_criteria>
<done>mod.rs declares odb_lifecycle submodule, inline lifecycle blocks replaced with method calls, relocated tests removed, LOC under 1800, clippy clean, all tests pass</done>
</task>

</tasks>

<verification>
1. `cargo clippy --all-targets -- -D warnings` — zero warnings (VER-01)
2. `cargo test -p flowsurface --lib -- chart::kline` — all tests pass including relocated ones (VER-02)
3. `wc -l src/chart/kline/mod.rs` — below 1800 (KLINE-03)
4. `grep -c "unsafe" src/chart/kline/odb_lifecycle.rs` — zero (VER-03)
5. `grep "pub(super) fn" src/chart/kline/odb_lifecycle.rs` — shows 4 methods (KLINE-02)
</verification>

<success_criteria>

- odb_lifecycle.rs exists with 4 pub(super) lifecycle methods (watchdog, sentinel, viewport digest, telemetry)
- kline/mod.rs below 1800 LOC
- All existing + relocated tests pass
- cargo clippy clean, no unsafe, no behavior changes
  </success_criteria>

<output>
After completion, create `.planning/phases/07-kline-odb-lifecycle-extraction/07-01-SUMMARY.md`
</output>
