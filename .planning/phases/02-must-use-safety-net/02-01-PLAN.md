---
phase: 02-must-use-safety-net
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screen/dashboard/pane.rs
  - src/chart.rs
  - src/screen/dashboard/panel.rs
  - src/screen/dashboard/sidebar.rs
  - src/screen/dashboard/tickers_table.rs
  - src/chart/comparison.rs
  - src/chart/kline/mod.rs
autonomous: true
requirements: [QUAL-02, VER-01, VER-02, VER-03]

must_haves:
  truths:
    - "Compiler warns when Effect/Action return values are silently dropped"
    - "Existing code compiles clean with no new warnings (all returns already handled correctly)"
    - "Zero behavior changes -- purely compile-time lint annotations"
    - "No new unsafe code introduced"
  artifacts:
    - path: "src/screen/dashboard/pane.rs"
      provides: "#[must_use] on Effect and Action enums + update/invalidate/tick/show_modal_with_focus functions"
      contains: "#[must_use"
    - path: "src/chart.rs"
      provides: "#[must_use] on chart::Action enum"
      contains: "#[must_use"
    - path: "src/screen/dashboard/panel.rs"
      provides: "#[must_use] on panel::Action enum + trait invalidate method"
      contains: "#[must_use"
    - path: "src/screen/dashboard/sidebar.rs"
      provides: "#[must_use] on sidebar::Action enum"
      contains: "#[must_use"
    - path: "src/screen/dashboard/tickers_table.rs"
      provides: "#[must_use] on tickers_table::Action enum + update function"
      contains: "#[must_use"
    - path: "src/chart/comparison.rs"
      provides: "#[must_use] on comparison::Action enum + update/open_editor_for_ticker functions"
      contains: "#[must_use"
    - path: "src/chart/kline/mod.rs"
      provides: "#[must_use] on set_basis/invalidate functions"
      contains: "#[must_use"
  key_links:
    - from: "src/screen/dashboard.rs"
      to: "src/screen/dashboard/pane.rs"
      via: "let _ = state.invalidate(Instant::now()) at line 1315"
      pattern: "let _ = state\\.invalidate"
---

<objective>
Add `#[must_use]` annotations to all Action and Effect enums and their Option-returning functions across pane.rs, dashboard.rs, chart.rs, panel.rs, sidebar.rs, tickers_table.rs, comparison.rs, and kline/mod.rs.

Purpose: Safety net before phases 4-7 move code between modules. Dropped Task/Effect/Action returns would silently break event dispatch -- compiler must catch these.
Output: All 7 Action/Effect enums annotated, all ~10 Option-returning functions annotated, clippy passes clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-must-use-safety-net/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate Action/Effect enums with #[must_use]</name>
  <files>
    src/screen/dashboard/pane.rs,
    src/chart.rs,
    src/screen/dashboard/panel.rs,
    src/screen/dashboard/sidebar.rs,
    src/screen/dashboard/tickers_table.rs,
    src/chart/comparison.rs
  </files>
  <read_first>
    src/screen/dashboard/pane.rs (lines 47-68 for Effect and Action enums),
    src/chart.rs (lines 59-63 for chart::Action enum),
    src/screen/dashboard/panel.rs (lines 17-27 for panel::Action enum and Panel trait),
    src/screen/dashboard/sidebar.rs (lines 30-37 for sidebar::Action enum),
    src/screen/dashboard/tickers_table.rs (lines 56-62 for tickers_table::Action enum),
    src/chart/comparison.rs (lines 16-21 for comparison::Action enum),
    .planning/phases/02-must-use-safety-net/02-RESEARCH.md (target inventory and patterns)
  </read_first>
  <action>
Add `#[must_use]` attribute to 7 enum definitions. Exact edits:

1. **src/screen/dashboard/pane.rs line 47** -- Add `#[must_use = "Effect must be handled by the caller"]` above `#[derive(Debug, Clone)]` before `pub enum Effect {`

2. **src/screen/dashboard/pane.rs line 63** -- Add `#[must_use = "Action must be handled by the caller"]` above `pub enum Action {`

3. **src/chart.rs line 60** -- Add `#[must_use = "Action must be handled by the caller"]` above `pub enum Action {`

4. **src/screen/dashboard/panel.rs line 17** -- Add `#[must_use = "Action must be handled by the caller"]` above `pub enum Action {}`

5. **src/screen/dashboard/sidebar.rs line 30** -- Add `#[must_use = "Action must be handled by the caller"]` above `pub enum Action {`

6. **src/screen/dashboard/tickers_table.rs line 56** -- Add `#[must_use = "Action must be handled by the caller"]` above `pub enum Action {`

7. **src/chart/comparison.rs line 16** -- Add `#[must_use = "Action must be handled by the caller"]` above `pub enum Action {`

Additionally, annotate the **Panel trait method** for forward safety:

1. **src/screen/dashboard/panel.rs line 24** -- Add `#[must_use = "returned Action must be handled"]` above `fn invalidate(&mut self, now: Option<Instant>) -> Option<Action>;`

Do NOT annotate `iced::Task` (already has `#[must_use]` in iced_runtime).
Do NOT annotate modal Action enums (layout_manager, network_manager, theme_editor, stream, settings, mini_tickers_list, column_drag) -- these are consumed inline in modal update handlers and are not at risk during the god-module extractions in phases 4-7.
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && grep -n '#\[must_use' src/screen/dashboard/pane.rs src/chart.rs src/screen/dashboard/panel.rs src/screen/dashboard/sidebar.rs src/screen/dashboard/tickers_table.rs src/chart/comparison.rs | wc -l</automated>
</verify>
<acceptance_criteria> - src/screen/dashboard/pane.rs contains `#[must_use = "Effect must be handled by the caller"]` before `pub enum Effect {` - src/screen/dashboard/pane.rs contains `#[must_use = "Action must be handled by the caller"]` before `pub enum Action {` - src/chart.rs contains `#[must_use = "Action must be handled by the caller"]` before `pub enum Action {` - src/screen/dashboard/panel.rs contains `#[must_use = "Action must be handled by the caller"]` before `pub enum Action {}` - src/screen/dashboard/panel.rs contains `#[must_use = "returned Action must be handled"]` before `fn invalidate(` - src/screen/dashboard/sidebar.rs contains `#[must_use = "Action must be handled by the caller"]` before `pub enum Action {` - src/screen/dashboard/tickers_table.rs contains `#[must_use = "Action must be handled by the caller"]` before `pub enum Action {` - src/chart/comparison.rs contains `#[must_use = "Action must be handled by the caller"]` before `pub enum Action {` - Total count of `#[must_use` lines across these 6 files is exactly 8
</acceptance_criteria>
<done>All 7 Action/Effect enums and 1 trait method annotated with #[must_use]</done>
</task>

<task type="auto">
  <name>Task 2: Annotate Option-returning functions with #[must_use] and verify clippy</name>
  <files>
    src/screen/dashboard/pane.rs,
    src/chart/kline/mod.rs,
    src/chart/comparison.rs,
    src/screen/dashboard/tickers_table.rs
  </files>
  <read_first>
    src/screen/dashboard/pane.rs (lines 1135-1145 for update fn, lines 1750-1760 for show_modal_with_focus fn, lines 1773-1780 for invalidate fn, lines 1813-1820 for tick fn),
    src/chart/kline/mod.rs (lines 910-920 for set_basis fn, lines 1133-1143 for invalidate fn),
    src/chart/comparison.rs (lines 88-96 for update fn),
    src/screen/dashboard/tickers_table.rs (lines 155-163 for update fn),
    src/screen/dashboard.rs (line 1315 for known intentional drop -- must already have `let _ =`)
  </read_first>
  <action>
Add `#[must_use]` to all functions returning `Option<Effect>` or `Option<Action>` that cross module boundaries. Critical: `Option<T>` does NOT inherit `#[must_use]` from `T`, so function-level annotation is required.

Exact edits:

**src/screen/dashboard/pane.rs:**

1. Above `pub fn update(&mut self, msg: Event) -> Option<Effect>` (~line 1139) -- Add `#[must_use = "returned Effect must be dispatched to dashboard"]`
2. Above `fn show_modal_with_focus(&mut self, requested_modal: Modal) -> Option<Effect>` (~line 1753) -- Add `#[must_use = "returned Effect must be dispatched to dashboard"]`
3. Above `pub fn invalidate(&mut self, now: Instant) -> Option<Action>` (~line 1776) -- Add `#[must_use = "returned Action must be dispatched"]`
4. Above `pub fn tick(&mut self, now: Instant, timezone: UserTimezone) -> Option<Action>` (~line 1816) -- Add `#[must_use = "returned Action must be dispatched"]`

**src/chart/kline/mod.rs:** 5. Above `pub fn set_basis(&mut self, new_basis: Basis) -> Option<Action>` (~line 916) -- Add `#[must_use = "returned Action must be dispatched"]` 6. Above `pub fn invalidate(&mut self, now: Option<Instant>) -> Option<Action>` (~line 1138) -- Add `#[must_use = "returned Action must be dispatched"]`

Do NOT annotate `missing_data_task()` (line 655) -- it is private (`fn`, not `pub fn`) and called in a single place within the same module.

**src/chart/comparison.rs:** 7. Above `pub fn update(&mut self, message: Message) -> Option<Action>` (~line 92) -- Add `#[must_use = "returned Action must be dispatched"]`

Do NOT annotate `open_editor_for_ticker()` (line 388) -- it is private.

**src/screen/dashboard/tickers_table.rs:** 8. Above `pub fn update(&mut self, message: Message) -> Option<Action>` (~line 159) -- Add `#[must_use = "returned Action must be dispatched"]`

**Verification of known intentional drop:**
Confirm `src/screen/dashboard.rs` line 1315 already uses `let _ = state.invalidate(Instant::now());` -- this suppresses the warning correctly. No change needed there.

After all annotations, run `cargo clippy --all-targets -- -D warnings` and fix any warnings. The only expected issue is the `invalidate_all_panes` intentional drop which is already guarded by `let _ =`. If unexpected warnings appear from other call sites, add `let _ =` with a comment explaining why the drop is intentional, or fix the actual bug (the return was being silently dropped when it shouldn't have been).
</action>
<verify>
<automated>cd /Users/terryli/fork-tools/flowsurface && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
</verify>
<acceptance*criteria> - src/screen/dashboard/pane.rs contains 4 lines matching `#[must_use = "returned` (on update, show_modal_with_focus, invalidate, tick) - src/chart/kline/mod.rs contains 2 lines matching `#[must_use = "returned` (on set_basis, invalidate) - src/chart/comparison.rs contains 1 line matching `#[must_use = "returned` (on update) - src/screen/dashboard/tickers_table.rs contains 1 line matching `#[must_use = "returned` (on update) - `cargo clippy --all-targets -- -D warnings` exits with code 0 - `grep -rn 'unsafe' src/ --include='*.rs'` output is identical before and after (VER-03) - src/screen/dashboard.rs line 1315 still contains `let * = state.invalidate(Instant::now())`
</acceptance_criteria>
<done>All 8 Option-returning public functions annotated, clippy passes clean with zero warnings, zero behavior changes, zero new unsafe code</done>
</task>

</tasks>

<verification>
Phase-level verification:

1. `cargo clippy --all-targets -- -D warnings` passes clean (VER-01)
2. `grep -c '#\[must_use' src/screen/dashboard/pane.rs` returns 6 (2 enums + 4 functions)
3. `grep -c '#\[must_use' src/chart.rs` returns 1 (1 enum)
4. `grep -c '#\[must_use' src/screen/dashboard/panel.rs` returns 2 (1 enum + 1 trait method)
5. `grep -c '#\[must_use' src/screen/dashboard/sidebar.rs` returns 1 (1 enum)
6. `grep -c '#\[must_use' src/screen/dashboard/tickers_table.rs` returns 2 (1 enum + 1 function)
7. `grep -c '#\[must_use' src/chart/comparison.rs` returns 2 (1 enum + 1 function)
8. `grep -c '#\[must_use' src/chart/kline/mod.rs` returns 2 (2 functions)
9. Total: 16 `#[must_use]` annotations across 7 files
10. `grep -rn 'unsafe' src/ --include='*.rs'` unchanged from before (VER-03)
    </verification>

<success_criteria>

- 16 `#[must_use]` annotations added across 7 files (7 enums + 1 trait method + 8 functions)
- `cargo clippy --all-targets -- -D warnings` exits 0
- Zero behavior changes (compile-time only)
- Zero new unsafe code
- Intentionally dropping a Task/Effect/Action without `let _ =` would now trigger a compiler warning
  </success_criteria>

<output>
After completion, create `.planning/phases/02-must-use-safety-net/02-01-SUMMARY.md`
</output>
