Launch the Flowsurface.app bundle on macOS via mise. Run: `mise run run:app`

This runs preflight checks (SSH tunnel + ClickHouse connectivity + data validation) then opens the .app bundle with dock icon.
